using DG.Tweening;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DOTweenTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Sequence mySeq = DOTween.Sequence();
        // mySeq.Append(transform.DOMoveY(200, 5));
        // mySeq.Append(transform.DOScale(new Vector3(2, 2, 2), 5));
        // mySeq.Append(transform.DORotate(new Vector3(0, 0, 180), 5));
        // mySeq.Insert(0, transform.DOMoveX(0, 5));
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
